import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('realEstateBuySell', '/en/buy-property-spain', 'en')

export default function Layout({ children }) {
  return children
}

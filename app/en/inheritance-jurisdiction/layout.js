import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('internationalInheritance', '/en/inheritance-jurisdiction', 'en')

export default function Layout({ children }) {
  return children
}

import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('realEstateInvestment', '/en/real-estate-investment', 'en')

export default function Layout({ children }) {
  return children
}

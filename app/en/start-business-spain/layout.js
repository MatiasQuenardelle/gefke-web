import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('startVirksomhed', '/en/start-business-spain', 'en')

export default function Layout({ children }) {
  return children
}

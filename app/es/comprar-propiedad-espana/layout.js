import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('realEstateBuySell', '/es/comprar-propiedad-espana', 'es')

export default function Layout({ children }) {
  return children
}

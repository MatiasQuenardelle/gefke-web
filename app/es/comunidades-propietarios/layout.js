import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('homeownerAssociations', '/es/comunidades-propietarios', 'es')

export default function Layout({ children }) {
  return children
}

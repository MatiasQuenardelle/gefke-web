import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('startVirksomhed', '/es/crear-empresa-espana', 'es')

export default function Layout({ children }) {
  return children
}

import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('internationalInheritance', '/es/herencias-internacionales', 'es')

export default function Layout({ children }) {
  return children
}

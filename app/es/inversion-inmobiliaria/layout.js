import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('realEstateInvestment', '/es/inversion-inmobiliaria', 'es')

export default function Layout({ children }) {
  return children
}

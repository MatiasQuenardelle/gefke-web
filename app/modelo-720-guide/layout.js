import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('modelo720Guide', '/modelo-720-guide', 'da')

export default function Modelo720GuideLayout({ children }) {
  return children
}

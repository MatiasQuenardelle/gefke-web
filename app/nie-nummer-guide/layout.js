import { getMetadata } from "@/lib/metadata"

export const metadata = getMetadata('nieNummerGuide', '/nie-nummer-guide', 'da')

export default function NieNummerGuideLayout({ children }) {
  return children
}
